{
  "name": "12-week-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "12-Week Year dashboard (Hebrew RTL) for tracking 12-week execution cycles with goals, tactics and partner accountability.",
  "workspaces": [
    "server",
    "client",
    "gym"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "dev:gym": "npm run dev --workspace gym",
    "build": "npm run build --workspace client && npm run build --workspace gym && npm run build --workspace server",
    "migrate": "npm run migrate --workspace server",
    "start": "npm run start --workspace server",
    "test": "npm run test --workspace server",
    "test:client": "npm run test --workspace client",
    "test:all": "npm run test --workspace server && npm run test --workspace client",
    "e2e": "bash scripts/test-e2e.sh",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace client",
    "backup": "bash scripts/backup.sh"
  },
  "engines": {
    "node": ">=18.18"
  },
  "license": "MIT"
}
